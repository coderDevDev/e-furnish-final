/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'kcoertpudvkgovylgsbn.supabase.co',
      'images.unsplash.com',
      'plus.unsplash.com'
    ]
  }
};

export default nextConfig;
